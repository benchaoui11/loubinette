import { UnavailablePage } from "@/components/shared/unavailable-page";

export const metadata = { title: "Conversion Funnel" };

export default function ConversionFunnelPage() {
  return <UnavailablePage eyebrow="Conversion Funnel" title="Funnel reporting foundation" body="Historical FirstIDP data supports application submission and basic visitor page views only. Future funnel steps will appear after event tracking is deployed." />;
}
