import { UnavailablePage } from "@/components/shared/unavailable-page";

export const metadata = { title: "Traffic Acquisition" };

export default function TrafficPage() {
  return <UnavailablePage eyebrow="Traffic Acquisition" title="Attribution workspace" body="UTM, click-id, first-touch, last-touch, AI referral, and campaign reporting begin after the privacy-aware first-party tracker is deployed." />;
}
